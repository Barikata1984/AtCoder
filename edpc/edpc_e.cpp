#include <iostream>#include <iterator>#include <vector>const int INF = 1e9;int main(){    long long n, w_limit, v_limit = 28;    std::cin >> n >> w_limit;    std::vector<long long> w(n), v(n);    for(int i = 0; i < n; ++i){        std::cin >> w.at(i) >> v.at(i);    }        std::vector<std::vector<long long>> dp(n + 1, std::vector<long long> (29, INF));    return 0;}