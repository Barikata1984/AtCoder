#include <iostream>#include <map>#include <vector>#include <algorithm>int main(){    int n, q;    std::cin >> n >> q;    std::string s;    std::cin >> s;    int start, end, count;    std::string buf;    std::vector<int> vec;    for(int i = 0; i < q; ++i){        std::cin >> start >> end;        count = 0;        for(int j = start - 1; j < end - 1; ++j){            buf = s.substr(j, 2);//            std::cout << "buf: " << buf << std::endl;            if("AC" == buf){                ++count;            }        }        std::cout << count << std::endl;    }    return 0;}