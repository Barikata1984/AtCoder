#include <iostream>#include <map>#include <vector>#include <algorithm>int main(){    int n, m;    std::cin >> n >> m;    int product;    std::map<int, std::vector<int>> drinks;    for(int i = 0; i < n; ++i){        std::cin >> a >> b;        drinks.insert(atd::make_pair(a * b, std::vector<int>{a, b}));    }    int sum = 0;    int ans = 0;    std::map<int, std;;vector<int>>::iterator itr = drinks.begin()    while(sum < m){        sum += *itr->second.at(1);        ++itr;    }        sum -= *(itr - 1)->second.at(1)    std::string str;    std::cout << "" << std::endl;    return 0;}