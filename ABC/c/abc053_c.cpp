#include <iostream>int main(){    long long x;    std::cin >> x;    long long ans = x / 11 * 2, rem = x % 11;    if(1 <= rem){        ++ans;    }    if(6 < rem){        ++ans;    }    std::cout << ans << std::endl;    return 0;}