#include <iostream>#include <vector>#include <algorithm>std::vector<vector<int>> combination(std::vector<int> readVec, int k){    int n = readVec.size();    std::vector<vector<int>> writeVec;    for(int i = 0; i < n; ++i){        int counter = 0;        std::vector<int> bufVec;        while(){        }        writeVec.push_back(bufVec);    }}int main(){    int n, a;    std::vector<int> x(n);    for(int i = 0; i < n; ++i){        std::cin >> x.at(i);    }    std::sort(x.begin(), x.end());        int sum;    std::vector<long> answers;    for(int i = 0; i < n; ++i){        sum = 0;        while(sum < a){        }    }}