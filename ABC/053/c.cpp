#include <iostream>#include <map>#include <vector>#include <algorithm>int main(){    int x;    std::cin >> x;    std::vector<std::vector<int>> vec(6, std::vector<int>(4));    int count = 0;    int i = 6;    while(i < n){        if(0 == ++count % 2){            i += 5;        }else{            i += 6;        }    }    std::cout << count << std::endl;    return 0;}